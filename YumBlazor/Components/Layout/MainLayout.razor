@inherits LayoutComponentBase

<div class="d-flex flex-column min-vh-100">

    <!-- NAVBAR - Always on top, has Admin links built-in -->
    <NavBar />

    <!-- MAIN CONTENT -->
    <main class="flex-grow-1 overflow-auto">
        <article class="content px-3 py-2">
            @Body
        </article>
    </main>

</div>
